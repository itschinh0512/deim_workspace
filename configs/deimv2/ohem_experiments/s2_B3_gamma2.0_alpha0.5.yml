# ============================================================
# Strategy 2, Experiment B3: gamma=2.0 + mal_alpha=0.5
# Expected: Combines gamma tuning with explicit negative weighting
# ============================================================
__include__: ['../deimv2_hgnetv2_n_fisheye.yml']

output_dir: ./outputs/ohem_s2_B3_gamma2.0_alpha0.5

DEIMCriterion:
  gamma: 2.0
  mal_alpha: 0.5   # baseline is None (uses gamma-only)
  matcher:
    change_matcher: True
    iou_order_alpha: 4.0
    matcher_change_epoch: 136
